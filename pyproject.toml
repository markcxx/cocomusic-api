[project]
name = "cocomusic-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "httpx",
  "pydantic",
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
